# Handoff

La voz comercial vigente está descrita en `20_CUSTOMER_COPY.md`: no reintroducir etiquetas técnicas, grados ni instrucciones sobre scroll en la presentación para clientes. El movimiento se conserva.

Última corrección: leer `19_SCROLL_INPUT_FIX.md`. El scroll ya no depende de activar movimiento ni de preferencias antiguas; se corrigió también el ancestro de las pantallas sticky.

La versión vigente utiliza únicamente los tres GLB de `C:\Users\PLAYER666\Documents\3d`. Leer `18_FULLSCREEN_PRESENTATION.md` para la composición de una pantalla por modelo y `17_THREE_MODEL_SCROLL.md` para origen y archivos retirados. El catálogo anterior de doce modelos quedó archivado.

## Ejecución

```powershell
cd D:\DevsManager
npm install
npm run dev
```

## Producción local

```powershell
npm run build
npm run preview
```

## Estado

La página principal 3D está implementada. Las páginas Servicios, Proceso, Sobre mí y Contacto se conservan. El despliegue público no forma parte de esta entrega.
